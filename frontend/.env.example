# Copy this to .env.local for local dev (leave blank — Vite proxy handles it)
VITE_API_URL=

# Production — set this in Vercel dashboard
# VITE_API_URL=https://mandibook-backend.railway.app

# Backend route prefix (optional, defaults to /api/v1)
# VITE_API_PREFIX=/api/v1
